// Concatenación de strings normal
let hello = 'Hello';
let world = 'World';
let epicPhrase = hello + ' ' + world + '!';
console.log(epicPhrase);

// Template literals
let epicPhrase2 = `${hello} ${world}!`;
console.log(epicPhrase2);

// Multi-line strings
let lorem = 'Esta es una linea \n' + 'esta es otra linea';
let lorem2 = `Esta es la primera linea
  esta es la segunda linea
`;

console.log(lorem);
console.log(lorem2);