// Función normal
function square (num) {
    return num * num;
}

// Arrow function
const square = (num) => {
    return num * num;
}

// No es necesario definir llaves ni tampoco return ya que implicitamente lo tiene.
const square = num => num * num;